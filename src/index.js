import React from 'react';
import ReactDOM from 'react-dom/client';
import LoadMainPage from './pages/main_page.jsx';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <LoadMainPage />  
);

